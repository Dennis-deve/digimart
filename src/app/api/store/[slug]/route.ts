import {NextResponse} from 'next/server';import {prisma} from '@/lib/db';
export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;const reseller=await prisma.reseller.findUnique({where:{storeSlug:slug},include:{User:{select:{phone:true}}}});if(!reseller||reseller.status!=='APPROVED')return NextResponse.json({status:'error',message:'Store not found.'},{status:404});const products=await prisma.product.findMany({where:{inStock:true,isExcluded:false}});const markups=await prisma.resellerProductMarkup.findMany({where:{resellerId:reseller.id}});const markupByProduct=new Map(markups.map(m=>[m.productId,Number(m.markupPct)]));return NextResponse.json({status:'success',data:{store:{name:reseller.storeName,slug:reseller.storeSlug,phone:reseller.User.phone},products:products.map(p=>({id:p.id,name:p.name,category:p.category,network:p.network,basePrice:Number(p.basePrice),price:Number(p.basePrice)*(1+(markupByProduct.get(p.id)??Number(reseller.defaultMarkupPct))/100),images:p.images,inStock:p.inStock}))}})}
