import {NextResponse} from 'next/server';import {z} from 'zod';import {requireRole} from '@/lib/guards';import {prisma} from '@/lib/db';
const schema=z.object({name:z.string().min(2).max(160),description:z.string().max(3000).optional(),category:z.string().min(2).max(80),network:z.string().max(40).optional(),basePrice:z.number().positive(),stock:z.boolean().default(true),variablePrice:z.boolean().default(false),minAmount:z.number().positive().optional(),maxAmount:z.number().positive().optional(),images:z.array(z.string().url()).max(8).default([])});
export async function POST(request:Request){const guard=await requireRole(request,['ADMIN']);if(guard.response)return guard.response;const input=schema.safeParse(await request.json());if(!input.success)return NextResponse.json({status:'error',message:input.error.issues[0]?.message??'Invalid product.'},{status:400});const p=input.data;const product=await prisma.product.create({data:{id:crypto.randomUUID(),source:'ADMIN',sourceProductId:crypto.randomUUID(),name:p.name,description:p.description,network:p.network,category:p.category,basePrice:p.basePrice,images:p.images,inStock:p.stock,variablePrice:p.variablePrice,minAmount:p.minAmount,maxAmount:p.maxAmount,updatedAt:new Date()}});return NextResponse.json({status:'success',data:{id:product.id,name:product.name,basePrice:Number(product.basePrice)}},{status:201})}
