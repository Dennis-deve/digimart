import {NextResponse} from 'next/server';import {requireRole} from '@/lib/guards';import {prisma} from '@/lib/db';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const guard=await requireRole(request,['ADMIN']);if(guard.response)return guard.response;const {id}=await params;const reseller=await prisma.reseller.findUnique({where:{id}});if(!reseller)return NextResponse.json({status:'error',message:'Reseller application not found.'},{status:404});if(!reseller.feePaid)return NextResponse.json({status:'error',message:'Registration fee must be confirmed before approval.'},{status:409});const updated=await prisma.reseller.update({where:{id},data:{status:'APPROVED',approvedAt:new Date()}});await prisma.user.update({where:{id:updated.userId},data:{role:'RESELLER'}});return NextResponse.json({status:'success',data:{id:updated.id,status:updated.status,storeSlug:updated.storeSlug}})}
