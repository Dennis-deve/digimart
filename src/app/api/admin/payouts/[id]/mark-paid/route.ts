import {NextResponse} from 'next/server';import {z} from 'zod';import {requireRole} from '@/lib/guards';import {prisma} from '@/lib/db';
const schema=z.object({momoRef:z.string().min(4).max(150)});
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const guard=await requireRole(request,['ADMIN']);if(guard.response)return guard.response;const {id}=await params;const input=schema.safeParse(await request.json());if(!input.success)return NextResponse.json({status:'error',message:'Valid MoMo payout reference required.'},{status:400});const payout=await prisma.payout.findUnique({where:{id}});if(!payout)return NextResponse.json({status:'error',message:'Payout not found.'},{status:404});if(payout.status!=='PENDING')return NextResponse.json({status:'error',message:'This payout has already been processed.'},{status:409});const updated=await prisma.payout.update({where:{id},data:{status:'PAID',momoRef:input.data.momoRef,paidAt:new Date()}});return NextResponse.json({status:'success',data:{id:updated.id,status:updated.status,momoRef:updated.momoRef}})}
