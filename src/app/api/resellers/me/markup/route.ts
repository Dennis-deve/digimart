import {NextResponse} from 'next/server';import {z} from 'zod';import {requireRole} from '@/lib/guards';import {prisma} from '@/lib/db';
const schema=z.object({defaultMarkupPct:z.number().min(0).max(100)});
export async function PATCH(request:Request){const guard=await requireRole(request,['CUSTOMER','RESELLER']);if(guard.response)return guard.response;const input=schema.safeParse(await request.json());if(!input.success)return NextResponse.json({status:'error',message:'Markup must be between 0 and 100 percent.'},{status:400});const reseller=await prisma.reseller.findUnique({where:{userId:guard.session!.id}});if(!reseller||reseller.status!=='APPROVED')return NextResponse.json({status:'error',message:'Approved reseller account required.'},{status:403});const updated=await prisma.reseller.update({where:{id:reseller.id},data:{defaultMarkupPct:input.data.defaultMarkupPct}});return NextResponse.json({status:'success',data:{defaultMarkupPct:Number(updated.defaultMarkupPct)}})}
