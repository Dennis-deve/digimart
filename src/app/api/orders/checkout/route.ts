import {NextResponse} from 'next/server';import {z} from 'zod';import {clientIp,limited} from '@/lib/rate-limit';import {initiateMoolreCollection} from '@/lib/providers';import {prisma} from '@/lib/db';import {readSession} from '@/lib/session';
const bodySchema=z.object({customerPhone:z.string().regex(/^0\d{9}$/,'Use a valid 10-digit Ghana phone number.'),provider:z.enum(['MTN','Telecel','AirtelTigo']),resellerSlug:z.string().optional(),items:z.array(z.object({productId:z.string(),qty:z.number().int().min(1).max(10),metadata:z.record(z.string(),z.unknown()).default({})})).min(1).max(20),idempotencyKey:z.string().min(12).max(200),couponCode:z.string().min(3).max(30).optional(),deliveryZoneId:z.string().optional(),deliveryMethod:z.enum(['DELIVERY','PICKUP','SELLER_SHIPPING','SCHEDULED_SERVICE']).optional(),deliveryAddressId:z.string().optional()});
const orderNo=()=>`DM-${crypto.randomUUID().slice(0,8).toUpperCase()}`;
export async function POST(request:Request){const gate=limited(`checkout:${clientIp(request)}`,5,60_000);if(!gate.allowed)return NextResponse.json({status:'error',message:'Too many checkout attempts. Please wait before trying again.'},{status:429,headers:{'Retry-After':String(gate.retryAfter)}});const parsed=bodySchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({status:'error',message:parsed.error.issues[0]?.message??'Invalid checkout request.'},{status:400});const token=request.headers.get('cookie')?.match(/digimart_session=([^;]+)/)?.[1];const session=token?await readSession(decodeURIComponent(token)):null;const products=await prisma.product.findMany({where:{id:{in:parsed.data.items.map(i=>i.productId)},inStock:true,isExcluded:false}});if(products.length!==parsed.data.items.length)return NextResponse.json({status:'error',message:'One or more selected products are unavailable.'},{status:409});let resellerId:string|undefined;if(parsed.data.resellerSlug){const reseller=await prisma.reseller.findUnique({where:{storeSlug:parsed.data.resellerSlug}});if(!reseller||reseller.status!=='APPROVED')return NextResponse.json({status:'error',message:'This reseller store is unavailable.'},{status:404});resellerId=reseller.id;}const subtotal=parsed.data.items.reduce((sum,item)=>sum+Number(products.find(p=>p.id===item.productId)!.basePrice)*item.qty,0);let discount=0;let couponCode:string|undefined;if(parsed.data.couponCode){const coupon=await prisma.coupon.findUnique({where:{code:parsed.data.couponCode.toUpperCase()}});const now=new Date();if(!coupon||!coupon.active||(coupon.startsAt&&coupon.startsAt>now)||(coupon.endsAt&&coupon.endsAt<now)||(coupon.usageLimit!==null&&coupon.usageCount>=coupon.usageLimit)||(coupon.minimumOrder&&subtotal<Number(coupon.minimumOrder)))return NextResponse.json({status:'error',message:'Coupon is not available for this order.'},{status:409});discount=coupon.discountType==='PERCENTAGE'?subtotal*Number(coupon.discountValue)/100:Math.min(subtotal,Number(coupon.discountValue));couponCode=coupon.code;}let deliveryFee=0;let deliveryZoneId:string|undefined;let deliveryAddressId:string|undefined;const deliveryMethod=parsed.data.deliveryMethod;if(deliveryMethod==='DELIVERY'){if(!parsed.data.deliveryZoneId||!parsed.data.deliveryAddressId)return NextResponse.json({status:'error',message:'Delivery zone and address are required for delivery.'},{status:400});const zone=await prisma.deliveryZone.findUnique({where:{id:parsed.data.deliveryZoneId}});const address=session?await prisma.customerAddress.findFirst({where:{id:parsed.data.deliveryAddressId,userId:session.id}}):null;if(!zone||!zone.active||!address)return NextResponse.json({status:'error',message:'Selected delivery zone or address is unavailable.'},{status:400});deliveryFee=zone.minimumOrder&&subtotal>=Number(zone.minimumOrder)?0:Number(zone.baseFee);deliveryZoneId=zone.id;deliveryAddressId=address.id;}else if(deliveryMethod==='PICKUP'){if(!parsed.data.deliveryZoneId)return NextResponse.json({status:'error',message:'Pickup zone is required.'},{status:400});const zone=await prisma.deliveryZone.findUnique({where:{id:parsed.data.deliveryZoneId}});if(!zone||!zone.active||!zone.pickupAvailable)return NextResponse.json({status:'error',message:'Pickup is unavailable for this zone.'},{status:400});deliveryZoneId=zone.id;}const total=Math.max(0,subtotal-discount)+deliveryFee;const id=orderNo();const paymentRef=`DM_order_${id}_${Date.now()}`;const order=await prisma.order.create({data:{id,customerId:session?.id||undefined,customerPhone:parsed.data.customerPhone,resellerId,total,discount,couponCode,deliveryFee,deliveryZoneId,deliveryMethod,deliveryAddressId,paymentRef,status:'PENDING',OrderItem:{create:parsed.data.items.map(item=>{const product=products.find(p=>p.id===item.productId)!;return {id:crypto.randomUUID(),Product:{connect:{id:product.id}},source:product.source,qty:item.qty,unitPrice:product.basePrice,basePrice:product.basePrice,metadata:item.metadata as never}})}}});try{const payment=await initiateMoolreCollection({payer:parsed.data.customerPhone,amount:total,channel:parsed.data.provider,externalref:paymentRef,reference:order.id});return NextResponse.json({status:'success',data:{orderId:order.id,status:'PENDING_PAYMENT',subtotal,discount,couponCode,deliveryFee,total,paymentReference:paymentRef,instructions:payment.instructions,mode:payment.mode,notice:'Do not mark this order paid until the verified Moolre webhook is processed.'}})}catch{await prisma.order.update({where:{id:order.id},data:{status:'FAILED'}});return NextResponse.json({status:'error',message:'Unable to start payment. Please try again later.'},{status:502})}}
