import {NextResponse} from 'next/server';import {requireRole} from '@/lib/guards';import {prisma} from '@/lib/db';
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const guard=await requireRole(request,['CUSTOMER','RESELLER','SELLER','RIDER','SUPPORT','ADMIN']);if(guard.response)return guard.response;const {id}=await params;const notification=await prisma.notification.findFirst({where:{id,userId:guard.session!.id}});if(!notification)return NextResponse.json({status:'error',message:'Notification not found.'},{status:404});const updated=await prisma.notification.update({where:{id},data:{readAt:new Date()}});return NextResponse.json({status:'success',data:{id:updated.id,readAt:updated.readAt}})}
