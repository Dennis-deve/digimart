'use client';
import {useState} from 'react';import Link from 'next/link';
export default function ResellerPage(){const [submitted,setSubmitted]=useState<{fee:number;slug:string}|null>(null);const [storeName,setStoreName]=useState('');const [storeSlug,setStoreSlug]=useState('');const [momo,setMomo]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const apply=async(e:React.FormEvent)=>{e.preventDefault();setError('');setBusy(true);try{const r=await fetch('/api/resellers/apply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({storeName,storeSlug,momoNumber:momo})});const data=await r.json();if(!r.ok)throw new Error(data.message);setSubmitted({fee:data.data.registrationFee,slug:data.data.storeSlug});}catch(e){setError(e instanceof Error?e.message:'Could not create application.')}finally{setBusy(false)}};return <main className="portal"><header><Link href="/" className="logo"><span>Digi</span><b>Mart</b></Link><Link href="/">← Back to marketplace</Link></header><section className="portalHero"><p>BUILD YOUR BUSINESS ON DIGIMART</p><h1>Open your own online store.</h1><span>Sell approved DigiMart products with your own branded store link, transparent markup rules and tracked earnings.</span></section>{submitted?<section className="successCard"><div>✓</div><h2>Your reseller application is ready for payment</h2><p>Your store URL will be <b>digimart.com/store/{submitted.slug}</b>. Pay the registration fee of <b>GH₵{submitted.fee.toFixed(2)}</b> to move from <b>Payment Pending</b> to <b>Under Review</b>. Approval is always controlled by DigiMart administrators.</p><button>Continue to secure registration payment</button></section>:<section className="application"><div><p className="eyebrow">RESELLER APPLICATION</p><h2>Tell us about your store</h2><p>Choose a store name and unique link. Your public store is created only after approval.</p></div><form onSubmit={apply}><label>Store name<input value={storeName} onChange={e=>setStoreName(e.target.value)} required placeholder="e.g. Bright Data Hub"/></label><label>Preferred store URL<input value={storeSlug} onChange={e=>setStoreSlug(e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,'').slice(0,60))} required placeholder="e.g. brightdatahub"/></label><label>Mobile Money number<input value={momo} onChange={e=>setMomo(e.target.value.replace(/\D/g,'').slice(0,10))} required inputMode="numeric" placeholder="055 123 4567"/></label><label>Location<select defaultValue=""><option value="" disabled>Select location</option><option>Kumasi, Ashanti</option><option>Accra, Greater Accra</option><option>Other Ghana location</option></select></label><label className="full">Business description<textarea placeholder="Tell us briefly what you plan to sell and who you serve."/></label>{error&&<p className="formError">{error}</p>}<button disabled={busy}>{busy?'Creating application…':'Continue to registration payment →'}</button></form></section>}<section className="steps"><div><b>1</b><h3>Apply</h3><p>Share your store details.</p></div><div><b>2</b><h3>Pay fee</h3><p>Registration payment is verified securely.</p></div><div><b>3</b><h3>Get approved</h3><p>DigiMart reviews your application.</p></div><div><b>4</b><h3>Start selling</h3><p>Share your personal store link.</p></div></section></main>}
