'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function NewProduct(){const r=useRouter();const [name,setName]=useState('');const [category,setCategory]=useState('');const [price,setPrice]=useState('');const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const res=await fetch('/api/admin/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,category,basePrice:Number(price),stock:true})});const data=await res.json();if(!res.ok)return setError(data.message);r.push('/admin/products')};return <main className="productAdmin"><header><div><p>ADMIN / CATALOG</p><h1>Add product</h1></div></header><section className="catalogTable"><form onSubmit={submit} className="productForm"><label>Product name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Category<input value={category} onChange={e=>setCategory(e.target.value)} required placeholder="e.g. Electronics"/></label><label>Base price (GH₵)<input value={price} onChange={e=>setPrice(e.target.value)} type="number" min="0.01" step="0.01" required/></label>{error&&<p className="formError">{error}</p>}<button>Create product</button></form></section></main>}
