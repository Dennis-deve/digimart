'use client';
import {useEffect,useState} from 'react';
type Ticket={id:string;topic:string;orderNo?:string;message:string;status:string;createdAt:string};
export default function AdminSupport(){const [tickets,setTickets]=useState<Ticket[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/support/tickets').then(r=>r.json()).then(r=>setTickets(r.data??[])).finally(()=>setLoading(false))},[]);return <main className="supportAdmin"><header><div><p>ADMIN / CUSTOMER CARE</p><h1>Support requests</h1></div><button>Support settings</button></header><section className="supportMetrics"><article><b>{tickets.length}</b><span>Open tickets</span></article><article><b>0</b><span>Assigned today</span></article><article><b>—</b><span>Average response time</span></article></section><section className="ticketList"><div className="heading"><div><p className="eyebrow">INBOX</p><h2>Customer requests</h2></div><button>Refresh</button></div>{loading?<p className="empty">Loading support requests…</p>:tickets.length?tickets.map(t=><article key={t.id}><div>◉</div><span><b>{t.topic.replaceAll('_',' ')}</b><small>{t.id}{t.orderNo?` · ${t.orderNo}`:''} · {new Date(t.createdAt).toLocaleString()}</small><p>{t.message}</p></span><em>{t.status}</em><button>Open →</button></article>):<p className="empty">No support requests yet. New tickets will appear here.</p>}</section></main>}
