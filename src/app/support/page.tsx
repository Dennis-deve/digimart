'use client';
import {useState} from 'react';import Link from 'next/link';
export default function Support(){const [sent,setSent]=useState('');const [topic,setTopic]=useState('');const [orderNo,setOrderNo]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);try{const result=await fetch('/api/support/tickets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic,orderNo,message})});const data=await result.json();if(!result.ok)throw new Error(data.message);setSent(data.data.id)}catch(error){alert(error instanceof Error?error.message:'Could not submit request.')}finally{setBusy(false)}};return <main className="support"><header><Link className="logo" href="/"><span>Digi</span><b>Mart</b></Link><Link href="/account">← Account</Link></header><section className="supportHero"><p>SUPPORT CENTER</p><h1>How can we help?</h1><span>Find quick answers, track an order, or send a request to DigiMart support.</span></section><section className="helpCards"><article><b>◴</b><h2>Track an order</h2><p>Check the latest payment, provider and delivery status.</p><button>Track order →</button></article><article><b>◉</b><h2>Payment or refund</h2><p>Get help with pending payment, wallet credit or refund status.</p><button>Payment help →</button></article><article><b>◌</b><h2>WhatsApp support</h2><p>Chat with a DigiMart agent through our official support channel.</p><button>Open WhatsApp →</button></article></section>{sent?<section className="supportSent"><div>✓</div><h2>Your support request has been received.</h2><p>Ticket <b>{sent}</b>. A support agent will review it and update you through your selected contact details.</p><button onClick={()=>{setSent('');setMessage('');setTopic('');setOrderNo('')}}>Submit another request</button></section>:<section className="ticket"><div><p className="eyebrow">SUPPORT REQUEST</p><h2>Send us the details</h2><p>Use an order number where possible. This helps support find the correct transaction faster.</p></div><form onSubmit={submit}><label>What do you need help with?<select value={topic} onChange={e=>setTopic(e.target.value)} required><option value="" disabled>Select a topic</option><option value="ORDER_TRACKING">Order tracking</option><option value="PAYMENT">Payment issue</option><option value="REFUND">Refund request</option><option value="DELIVERY">Delivery issue</option><option value="ACCOUNT">Account help</option></select></label><label>Order number (optional)<input value={orderNo} onChange={e=>setOrderNo(e.target.value.toUpperCase())} placeholder="e.g. DM-48291"/></label><label className="wide">Tell us what happened<textarea value={message} onChange={e=>setMessage(e.target.value)} required placeholder="Include the relevant details without sharing your MoMo PIN or passwords."/></label><button disabled={busy}>{busy?'Sending request…':'Submit support request'}</button></form></section>}</main>}
