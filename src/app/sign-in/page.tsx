'use client';
import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function SignIn(){const router=useRouter();const [phone,setPhone]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});const data=await r.json();if(!r.ok)throw new Error(data.message);router.push('/account');}catch(e){setError(e instanceof Error?e.message:'Could not sign in.')}finally{setBusy(false)}};return <main className="auth"><section className="authBrand"><div className="logo"><span>Digi</span><b>Mart</b></div><p>ONE MARKETPLACE. EVERY NEED.</p><h1>Welcome back to a simpler way to shop.</h1><span>Access your orders, wallet, reseller store and marketplace account in one secure place.</span><div className="authMarks"><b>🔒 Secure account access</b><b>◴ Order tracking</b><b>◉ Wallet & rewards</b></div></section><section className="authForm"><Link href="/">← Back to marketplace</Link><div><p className="eyebrow">SIGN IN</p><h2>Welcome back</h2><span>Enter your account details to continue.</span></div><form onSubmit={submit}><label>Phone number<input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,10))} inputMode="numeric" placeholder="055 123 4567" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Enter your password" required/></label><div className="authOptions"><label><input type="checkbox"/> Remember me</label><button type="button">Forgot password?</button></div>{error&&<p className="authError">{error}</p>}<button disabled={busy} className="authSubmit">{busy?'Signing in…':'Sign in to DigiMart'}</button><button type="button" className="google">G Continue with Google</button></form><p className="authSwitch">New to DigiMart? <Link href="/sign-up">Create a free account</Link></p></section></main>}
