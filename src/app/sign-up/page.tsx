'use client';
import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function SignUp(){const router=useRouter();const [phone,setPhone]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [terms,setTerms]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!terms)return setError('Please accept the terms and privacy policy.');setBusy(true);setError('');try{const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,email,password})});const data=await r.json();if(!r.ok)throw new Error(data.message);router.push('/account');}catch(e){setError(e instanceof Error?e.message:'Could not create account.')}finally{setBusy(false)}};return <main className="auth"><section className="authBrand"><div className="logo"><span>Digi</span><b>Mart</b></div><p>ONE MARKETPLACE. EVERY NEED.</p><h1>One account for everything you need.</h1><span>Shop, track orders, earn rewards, manage your wallet and grow into a seller or reseller with DigiMart.</span><div className="authMarks"><b>✓ Shop without limits</b><b>✓ Track every order</b><b>✓ Earn rewards</b></div></section><section className="authForm"><Link href="/">← Back to marketplace</Link><div><p className="eyebrow">CREATE ACCOUNT</p><h2>Join DigiMart</h2><span>It takes less than a minute to get started.</span></div><form onSubmit={submit}><label>Phone number<input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,10))} inputMode="numeric" placeholder="055 123 4567" required/></label><label>Email address <small>(optional)</small><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com"/></label><label>Create password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="At least 8 characters" required/></label><label className="terms"><input checked={terms} onChange={e=>setTerms(e.target.checked)} type="checkbox"/> I agree to DigiMart’s terms and privacy policy.</label>{error&&<p className="authError">{error}</p>}<button disabled={busy} className="authSubmit">{busy?'Creating account…':'Create my account'}</button><button type="button" className="google">G Continue with Google</button></form><p className="authSwitch">Already have an account? <Link href="/sign-in">Sign in</Link></p></section></main>}
